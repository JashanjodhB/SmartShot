A Simple Qt desktop app:
  -Takes a screenshot of the selected region 
  -Lets the user review and retake shots
  -Stores multiple screenshots
  -Transforms all saved screenshots into a single PDF File

Options to Run:
  -Download Qt and use the Qt Creator IDE to run the main.cpp file
  -Download and run the compiled executable 
